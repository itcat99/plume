import React, { Component } from "react";
import { createContainer } from "@plume/flow";

class NAME extends Component {
  render() {
    return <div>Hello NAME Container.</div>;
  }
}

export default createContainer(NAME);
